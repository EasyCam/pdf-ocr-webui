# PDF OCR WebUI 服务停止问题解决方案

## 🔍 问题描述

当PDF OCR WebUI服务无法正常停止时，可能出现以下情况：
- `systemctl stop` 命令无响应
- 进程仍在后台运行
- 端口5000仍被占用
- 服务重启失败

## 🛠️ 解决方案

### 方案一：改进的停止脚本（推荐）

使用改进后的停止脚本，它会：
1. 尝试优雅停止systemd服务
2. 强制终止systemd服务（如果需要）
3. 查找并终止残留进程
4. 释放占用的端口

```bash
./service_stop.sh
```

### 方案二：强力停止脚本

如果常规停止仍然失败，使用强力停止脚本：

```bash
./force_stop_service.sh
```

这个脚本会：
- 停止所有相关的systemd服务
- 查找并终止所有相关进程
- 释放所有占用的端口
- 清理临时文件
- 提供详细的状态报告

### 方案三：诊断后停止

先诊断问题，再选择合适的停止方法：

```bash
# 1. 诊断当前状态
./diagnose_service.sh

# 2. 根据诊断结果选择停止方法
./force_stop_service.sh
```

## 🔧 手动停止方法

如果脚本都无法使用，可以手动执行以下步骤：

### 1. 停止systemd服务

```bash
# 停止主服务
sudo systemctl stop pdf-ocr-webui.service

# 如果无响应，强制停止
sudo systemctl kill --signal=SIGKILL pdf-ocr-webui.service

# 停止监控服务
sudo systemctl stop pdf-ocr-webui-monitor.service
```

### 2. 查找并终止进程

```bash
# 查找Python进程
ps aux | grep -E "(python.*run\.py|python.*app\.py)"

# 终止进程（替换PID为实际进程ID）
kill -TERM <PID>

# 如果仍在运行，强制终止
kill -KILL <PID>
```

### 3. 释放端口

```bash
# 查找占用端口5000的进程
lsof -i :5000

# 终止占用端口的进程
kill -TERM <PID>
kill -KILL <PID>  # 如果需要强制终止
```

### 4. 清理临时文件

```bash
# 清理可能的临时文件
sudo rm -rf /tmp/*pdf-ocr*
sudo rm -rf /var/run/pdf-ocr-webui.pid
```

## 📊 常见停止失败原因

### 1. 进程卡死

**原因**: 应用进程处理大文件时卡死
**解决**: 使用强制终止信号 `kill -KILL`

### 2. 端口占用

**原因**: 进程异常退出但端口未释放
**解决**: 直接终止占用端口的进程

### 3. 子进程未清理

**原因**: 主进程退出但子进程仍在运行
**解决**: 查找所有相关进程并逐一终止

### 4. systemd服务配置问题

**原因**: 服务配置的超时时间过短
**解决**: 修改服务配置或使用强制停止

## 🔍 诊断工具

### 使用诊断脚本

```bash
./diagnose_service.sh
```

诊断脚本会检查：
- systemd服务状态
- 相关进程信息
- 端口占用情况
- 最近的日志
- 系统资源使用
- 网络连接状态

### 手动诊断命令

```bash
# 检查服务状态
systemctl status pdf-ocr-webui.service

# 检查进程
ps aux | grep python

# 检查端口
netstat -tuln | grep :5000
lsof -i :5000

# 检查日志
sudo journalctl -u pdf-ocr-webui.service -n 20

# 检查系统资源
free -h
top
```

## 🚀 预防措施

### 1. 优化服务配置

在systemd服务文件中设置合适的超时时间：

```ini
[Service]
TimeoutStopSec=30
KillMode=mixed
KillSignal=SIGINT
```

### 2. 监控资源使用

定期检查：
- 内存使用情况
- CPU负载
- 磁盘空间

### 3. 定期重启

如果服务长期运行，建议定期重启：

```bash
# 每天凌晨重启（可选）
0 2 * * * /path/to/service_restart.sh
```

## 📋 完整的停止流程

### 标准停止流程

1. **尝试正常停止**：
   ```bash
   ./service_stop.sh
   ```

2. **检查状态**：
   ```bash
   ./diagnose_service.sh
   ```

3. **如果仍在运行，强力停止**：
   ```bash
   ./force_stop_service.sh
   ```

4. **验证停止成功**：
   ```bash
   systemctl status pdf-ocr-webui.service
   netstat -tuln | grep :5000
   ```

### 紧急停止流程

如果需要立即停止服务：

```bash
# 一键强力停止
./force_stop_service.sh

# 或手动执行
sudo systemctl kill --signal=SIGKILL pdf-ocr-webui.service
sudo pkill -f "run.py"
sudo kill -KILL $(lsof -ti :5000)
```

## ⚠️ 注意事项

1. **数据安全**: 强制停止可能导致正在处理的任务丢失
2. **等待时间**: 给进程足够的时间优雅退出
3. **权限问题**: 某些操作需要sudo权限
4. **服务依赖**: 注意停止可能影响其他依赖服务

## 🔄 重启服务

停止服务后，重新启动：

```bash
# 启动服务
./service_start.sh

# 或直接重启
./service_restart.sh
```

## 📞 获取帮助

如果问题仍然存在：

1. 查看详细日志：`./view_logs.sh`
2. 运行完整诊断：`./diagnose_service.sh`
3. 检查系统资源：`free -h && df -h`
4. 重启系统（最后手段） 