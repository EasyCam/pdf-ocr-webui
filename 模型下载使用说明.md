# PDF-Craft模型批量下载使用说明

## 🎯 问题背景

您遇到的错误信息：
```
PDF-Craft处理失败: An error happened while trying to locate the files on the Hub and we cannot find the appropriate snapshot folder for the specified revision on the local disk.
```

这是因为PDF-Craft在运行时尝试从Hugging Face Hub下载模型文件失败导致的。

## 🚀 解决方案

我们创建了两个脚本来彻底解决这个问题：

### 1. 模型批量下载脚本 (`download_all_models.sh`)
- **功能**: 一次性下载所有PDF-Craft需要的模型文件
- **优势**: 避免运行时下载失败，支持断点续传和重试机制

### 2. 模型下载问题修复脚本 (`fix_model_download_issue.sh`)
- **功能**: 修复模型下载环境，配置缓存路径
- **优势**: 解决网络和环境配置问题

## 📋 使用步骤

### Step 1: 运行模型批量下载脚本

```bash
# 设置执行权限
chmod +x download_all_models.sh

# 运行下载脚本
./download_all_models.sh
```

### Step 2: 如果下载失败，运行修复脚本

```bash
# 设置执行权限
chmod +x fix_model_download_issue.sh

# 运行修复脚本
./fix_model_download_issue.sh
```

### Step 3: 重启服务

```bash
# 使用修复后的重启脚本
./restart_with_fix.sh

# 或者手动重启
./force_stop_service.sh
./service_start.sh
```

## 🔧 脚本功能详解

### `download_all_models.sh` 功能特性

1. **智能下载策略**
   - 首先尝试直接初始化PDF-Craft触发自动下载
   - 如失败，则手动下载常用模型列表
   - 支持断点续传和自动重试

2. **环境配置**
   - 自动设置所有必要的环境变量
   - 创建正确的缓存目录结构
   - 支持Hugging Face镜像配置

3. **模型类型覆盖**
   - **LayoutLM系列**: 文档布局理解模型
   - **DiT系列**: 文档图像Transformer模型
   - **表格检测模型**: 表格结构识别
   - **OCR模型**: 文本识别模型
   - **ONNX模型**: PaddleOCR优化模型

4. **验证和统计**
   - 下载完成后自动验证模型完整性
   - 生成详细的下载统计报告
   - 创建模型清单文件

### `fix_model_download_issue.sh` 功能特性

1. **网络诊断**
   - 检查基本网络连接
   - 测试Hugging Face连通性
   - 提供网络问题解决建议

2. **环境修复**
   - 清理损坏的模型缓存
   - 重新配置环境变量
   - 设置正确的缓存路径

3. **镜像配置**
   - 支持配置Hugging Face镜像源
   - 提供代理设置选项
   - 支持离线模式配置

## 📊 下载内容说明

### 核心模型文件
- **microsoft/layoutlm-base-uncased**: 文档布局理解基础模型
- **microsoft/layoutlmv2-base-uncased**: 文档布局理解v2模型
- **microsoft/dit-base-finetuned-rvlcdip**: 文档图像分类模型
- **microsoft/table-transformer-detection**: 表格检测模型
- **microsoft/trocr-base-printed**: 印刷文本OCR模型

### ONNX优化模型
- **PaddleOCR检测模型**: 文本区域检测
- **PaddleOCR识别模型**: 文本内容识别
- **PaddleOCR分类模型**: 文本方向分类

### 预计下载大小
- **总大小**: 约5-10GB（取决于网络和可用模型）
- **磁盘要求**: 至少10GB可用空间
- **网络要求**: 稳定的互联网连接

## 🌐 网络优化建议

### 如果下载速度慢或失败

1. **配置镜像源**
   ```bash
   export HF_ENDPOINT="https://hf-mirror.com"
   ./download_all_models.sh
   ```

2. **配置代理**
   ```bash
   export HTTP_PROXY="http://your-proxy:port"
   export HTTPS_PROXY="http://your-proxy:port"
   ./download_all_models.sh
   ```

3. **启用离线模式**（模型已下载后）
   ```bash
   export PDF_CRAFT_OFFLINE=1
   ./restart_with_fix.sh
   ```

## 📁 文件结构说明

下载完成后，模型文件将按以下结构存储：

```
models/
├── .cache/
│   ├── huggingface/          # Hugging Face模型缓存
│   ├── transformers/         # Transformers模型缓存
│   └── torch/               # PyTorch模型缓存
├── ppocrv4/                 # PaddleOCR ONNX模型
│   ├── det/                 # 检测模型
│   ├── rec/                 # 识别模型
│   └── cls/                 # 分类模型
└── model_inventory.txt      # 模型清单文件
```

## 🔍 验证和测试

### 检查模型状态
```bash
python3 model_config.py
```

### 测试服务
```bash
./diagnose_service.sh
```

### 查看下载统计
```bash
du -sh models/
find models/ -name "*.bin" -o -name "*.safetensors" -o -name "*.onnx" | wc -l
```

## ❓ 常见问题解决

### Q1: 下载中断怎么办？
**A**: 脚本支持断点续传，重新运行即可从中断处继续下载。

### Q2: 磁盘空间不足？
**A**: 脚本会自动检查磁盘空间，建议确保至少10GB可用空间。

### Q3: 网络连接问题？
**A**: 先运行 `fix_model_download_issue.sh` 诊断和修复网络问题。

### Q4: 某些模型下载失败？
**A**: 脚本会跳过不可用的模型，不影响其他模型下载和使用。

### Q5: 如何更新模型？
**A**: 重新运行 `download_all_models.sh` 即可更新到最新版本。

## 🎉 完成后的效果

运行脚本后，您将获得：

1. ✅ **完整的模型库**: 所有PDF-Craft需要的模型已下载
2. ✅ **正确的配置**: 环境变量和缓存路径已设置
3. ✅ **离线能力**: 无需依赖网络即可正常运行
4. ✅ **性能优化**: 避免运行时下载延迟
5. ✅ **稳定性提升**: 消除网络问题导致的处理失败

## 🚀 立即开始

```bash
# 一键解决模型问题
chmod +x download_all_models.sh fix_model_download_issue.sh
./download_all_models.sh
./restart_with_fix.sh
```

完成后，您的PDF-Craft服务将能够稳定运行，不再出现模型下载相关的错误！ 